# meeting-transcriber
It transcribes meeting sessions with local agent

## Platforms

- macOS path: `transcribe.py`, `test_audio.py`, and `audio_route.swift` use BlackHole/Core Audio.
- Windows 11 + NVIDIA path: see `WINDOWS_NVIDIA.md`, `transcribe_windows.py`, and `test_audio_windows.py`.
